# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# MongoDB Atlas Connection String
# Replace <username>, <password>, <cluster-address> with your MongoDB Atlas credentials
DATABASE_URL="mongodb+srv://<username>:<password>@<cluster-address>/emptrack?retryWrites=true&w=majority"

# NextAuth
# Generate a secure secret: use `openssl rand -base64 32` or https://generate-secret.vercel.app/32
NEXTAUTH_SECRET="your-secret-key-here-min-32-chars"
NEXTAUTH_URL="http://localhost:3000"

# GitHub OAuth (optional, for GitHub login and activity integration)
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET="" 